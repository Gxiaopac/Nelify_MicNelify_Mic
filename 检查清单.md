# 问题检查清单

## 当前问题
API 请求返回 HTML（`<!DOCTYPE`）而不是 JSON，说明请求没有到达 Netlify Functions。

## 请按以下步骤检查

### 1. 检查浏览器 Network 面板（最重要！）

1. 打开你的网站
2. 按 **F12** 打开开发者工具
3. 切换到 **"Network"** 标签
4. **清空**之前的请求记录
5. 刷新页面或触发一个 API 请求（比如点击"开始测试"）
6. 找到失败的请求（通常是 `/api/config` 或 `/api/analyze`）

**请告诉我**：
- 请求的**完整 URL** 是什么？（例如：`https://xxx.netlify.app/api/config`）
- **状态码**是什么？（200, 404, 500 等）
- **响应内容**的前几行是什么？（可以截图）

### 2. 检查 Netlify Dashboard

1. 登录 Netlify Dashboard
2. 进入你的站点
3. 点击左侧 **"Functions"** 标签

**请告诉我**：
- 是否能看到函数列表？
- 如果能看到，有哪些函数？（应该看到 `api`, `test`, `api-simple`）
- 如果看不到，说明 Functions 没有部署

### 3. 检查构建日志

1. 在 Netlify Dashboard 中，点击 **"Deploys"** 标签
2. 点击最新的部署
3. 查看构建日志

**请告诉我**：
- `pip install -r requirements.txt` 是否成功？
- 是否有任何 Python 相关的错误？
- 是否看到 "Installing functions" 或类似的消息？

### 4. 直接测试 Functions（在浏览器中）

访问以下 URL（替换为你的实际域名）：

```
https://你的域名.netlify.app/.netlify/functions/test
```

**请告诉我**：
- 返回了什么内容？
- 如果是 JSON，说明 Functions 工作正常
- 如果是 404 或 HTML，说明 Functions 没有部署

### 5. 检查 Functions 日志

1. 在 Netlify Dashboard → Functions → api
2. 点击 **"Logs"** 标签
3. 触发一个 API 请求
4. 查看日志

**请告诉我**：
- 是否有任何日志输出？
- 是否有错误信息？

## 根据检查结果判断

### 如果 Network 面板显示请求 URL 是 `/api/config`
- 说明前端代码正确
- 问题在 Netlify 的重定向或 Functions 部署

### 如果 Network 面板显示请求 URL 是 `/api/api/config`
- 说明前端代码有路径重复问题
- 需要修复 `API_BASE` 配置

### 如果 Functions 列表为空
- 说明 Functions 没有部署
- 需要检查构建配置和日志

### 如果直接访问 `/.netlify/functions/test` 返回 404
- 说明 Functions 没有部署
- 需要检查 `netlify.toml` 配置

## 请优先提供

1. **Network 面板中的请求详情**（最重要！）
2. **Netlify Dashboard → Functions 中是否能看到函数**
3. **直接访问 `/.netlify/functions/test` 的结果**

有了这些信息，我就能快速定位并修复问题！

