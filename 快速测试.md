# 快速测试指南

## 测试步骤

### 1. 测试简单函数（最重要！）

在浏览器中访问（替换为你的域名）：
```
https://你的域名.netlify.app/.netlify/functions/test
```

**预期结果**：
- ✅ 如果返回 JSON：`{"status": "success", "message": "Netlify Functions 工作正常！"}`
  - 说明 Functions 部署正常，问题在路径处理或 Flask 应用
- ❌ 如果返回 404 或 HTML：
  - 说明 Functions 没有部署，需要检查构建配置

### 2. 测试简化版 API

访问：
```
https://你的域名.netlify.app/.netlify/functions/api-simple
```

**预期结果**：
- ✅ 如果返回 JSON：说明 Functions 和路径处理都正常
- ❌ 如果返回 404：说明函数没有部署

### 3. 测试完整版 API（直接访问）

访问：
```
https://你的域名.netlify.app/.netlify/functions/api/api/config
```

**预期结果**：
- ✅ 如果返回 JSON 配置：说明 Flask 应用正常工作
- ❌ 如果返回错误：查看错误信息，可能是导入问题

### 4. 测试通过重定向访问

访问：
```
https://你的域名.netlify.app/api/config
```

**预期结果**：
- ✅ 如果返回 JSON：说明重定向和 Functions 都正常
- ❌ 如果返回 HTML（404 页面）：说明重定向没有工作

## 根据测试结果判断问题

### 情况 A：`/functions/test` 返回 404
**问题**：Functions 没有部署
**解决**：
1. 检查 Netlify Dashboard → Functions，看是否有函数列表
2. 检查构建日志，确认 `pip install` 是否成功
3. 确认 `netlify.toml` 中 `functions = "netlify/functions"` 配置正确

### 情况 B：`/functions/test` 返回 JSON，但 `/api/config` 返回 HTML
**问题**：重定向规则没有工作
**解决**：
1. 检查 `netlify.toml` 中的 redirect 规则
2. 检查 `_redirects` 文件是否存在
3. 尝试清除 Netlify 缓存并重新部署

### 情况 C：`/functions/api/api/config` 返回错误
**问题**：Flask 应用或 serverless-wsgi 有问题
**解决**：
1. 查看 Functions 日志（Dashboard → Functions → api → Logs）
2. 检查错误信息
3. 可能需要更新 `serverless-wsgi` 版本或修改路径处理逻辑

### 情况 D：所有测试都正常，但前端仍然报错
**问题**：前端 API 路径配置错误
**解决**：
1. 检查浏览器 Network 面板，看实际请求的 URL
2. 确认 `API_BASE` 配置正确
3. 检查是否有 CORS 问题

## 请告诉我测试结果

请按照上述步骤测试，并告诉我：
1. `/functions/test` 返回什么？
2. `/functions/api-simple` 返回什么？
3. `/functions/api/api/config` 返回什么？
4. `/api/config` 返回什么？

这样我可以快速定位问题！

