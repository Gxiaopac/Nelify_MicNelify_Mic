# Netlify 部署故障排查

## 问题：API 返回 HTML 而不是 JSON

如果看到错误：`Unexpected token '<', "<!DOCTYPE "... is not valid JSON`，说明 API 请求返回的是 HTML 页面（通常是 404 页面）而不是 JSON。

### 可能的原因和解决方案

#### 1. 检查 Netlify Functions 是否正确部署

在 Netlify Dashboard 中：
- 进入你的站点
- 点击 "Functions" 标签
- 确认看到 `api` 函数已部署
- 如果看不到，说明函数没有正确部署

**解决方案**：
- 检查 `netlify/functions/api.py` 文件是否存在
- 确认 `netlify.toml` 中 `functions = "netlify/functions"` 配置正确
- 重新触发部署

#### 2. 检查构建日志

在 Netlify Dashboard 中：
- 进入 "Deploys" 标签
- 查看最新的部署日志
- 查找是否有 Python 相关的错误
- 确认 `pip install -r requirements.txt` 成功执行

**常见问题**：
- `serverless-wsgi` 安装失败
- Python 版本不匹配
- 依赖包安装失败

#### 3. 检查函数路径

在浏览器中直接访问：
```
https://你的域名.netlify.app/.netlify/functions/api/api/config
```

如果返回 JSON，说明函数工作正常，但路径处理有问题。
如果返回 404 或错误，说明函数没有正确部署。

#### 4. 检查 redirect 规则

确认 `netlify.toml` 中的 redirect 规则：
```toml
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
```

或者使用 `_redirects` 文件（已创建）。

#### 5. 测试函数

在 Netlify Dashboard 中：
- 进入 "Functions" 标签
- 点击 `api` 函数
- 使用 "Test" 功能测试函数
- 查看返回结果

#### 6. 检查浏览器控制台

打开浏览器开发者工具（F12）：
- 查看 Network 标签
- 找到失败的 API 请求
- 查看请求 URL 和响应内容
- 确认请求路径是否正确

### 调试步骤

1. **确认函数部署**：
   - 访问 `https://你的域名.netlify.app/.netlify/functions/api/api/config`
   - 应该返回 JSON，不是 HTML

2. **检查路径处理**：
   - 如果直接访问函数可以工作，但通过 `/api/*` 不行
   - 说明 redirect 规则有问题
   - 检查 `netlify.toml` 和 `_redirects` 文件

3. **查看函数日志**：
   - 在 Netlify Dashboard → Functions → api → Logs
   - 查看是否有错误信息

4. **测试本地函数**（如果安装了 Netlify CLI）：
   ```bash
   netlify dev
   ```
   然后在本地测试 API 调用

### 快速修复

如果以上都不行，尝试：

1. **重新部署**：
   - 在 Netlify Dashboard 中点击 "Trigger deploy" → "Clear cache and deploy site"

2. **检查文件结构**：
   确保文件结构如下：
   ```
   MicTester_Netlify_Web/
   ├── app.py
   ├── config.json
   ├── index.html
   ├── netlify.toml
   ├── requirements.txt
   ├── runtime.txt
   ├── _redirects
   ├── netlify/
   │   └── functions/
   │       └── api.py
   └── static/
       ├── app.js
       └── style.css
   ```

3. **验证 requirements.txt**：
   确保包含：
   ```
   Flask==3.0.0
   flask-cors==4.0.0
   numpy>=1.24.0,<1.26.0
   scipy==1.10.1
   pandas==1.5.3
   openpyxl==3.1.2
   serverless-wsgi==3.0.4
   ```

4. **检查 Python 版本**：
   `runtime.txt` 应该包含：`3.11`

### 联系支持

如果问题仍然存在，请提供：
1. Netlify 构建日志
2. Netlify Functions 日志
3. 浏览器控制台错误信息
4. Network 标签中的请求详情

